import React from 'react';

const SearchBar = props => {
    return (
        <form>
            <input 
            type="text"
            placeholder="What to do next?"
            />
        </form>    
    )
}

export default SearchBar;
